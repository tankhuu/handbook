# Create multiple directories with 1 Command

```
mkdir -p htg/{articles,images,note,done}

mkdir -p htg/{articles/{new,rewrites},images,notes,done}

mkdir -p ~/Documents/htg/{articles/{new,rewrites},images,notes,done}
```